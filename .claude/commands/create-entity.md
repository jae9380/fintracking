## Role

Create domain entity (DDD)

## Rules

- No @Setter, use constructor/factory
- Keep logic inside entity
- Use @Convert(AesEncryptConverter) for sensitive fields
- Create unit test with entity
